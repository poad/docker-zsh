name: Docker build and push
 
on:
  push:
    branches:
    - main 
  pull_request:
    branches:
      - main
  schedule:
    - cron:  '0 0 * * 2'

  workflow_dispatch:

jobs:
  docker_build_push:
    strategy:
      matrix:
        versions: [
          { distribution: 'ubuntu', dist_version_code: 'noble', ubuntu_version_code: 'noble', tag: 'noble' },
          { distribution: 'ubuntu', dist_version_code: 'jammy', ubuntu_version_code: 'jammy', tag: 'jammy' },
          { distribution: 'ubuntu', dist_version_code: 'noble', ubuntu_version_code: 'noble', tag: 'latest' },
          { distribution: 'debian', dist_version_code: 'bookworm', ubuntu_version_code: 'jammy', tag: 'bookworm' }
        ]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - uses: actions/cache@v4
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-buildx-

    - name: Login to DockerHub
      if: github.event_name != 'pull_request' && github.actor != 'dependabot[bot]'
      uses: docker/login-action@v3 
      with:
        username: poad
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build
      if: github.event_name == 'pull_request' || github.actor == 'dependabot[bot]'
      uses: docker/build-push-action@v6
      with:
        push: false
        context: .
        platforms: linux/amd64,linux/arm64
        file: ${{ matrix.versions.distribution }}/Dockerfile
        tags: poad/docker-zsh:${{ matrix.versions.tag }}
        build-args: |
          DISTRIBUTION=${{ matrix.versions.distribution }}
          DIST_VERSION_CODE=${{ matrix.versions.dist_version_code }}
          UBUNTU_VERSION_CODE=${{ matrix.versions.ubuntu_version_code }}

    - name: Build and push
      if: github.event_name != 'pull_request' && github.actor != 'dependabot[bot]'
      uses: docker/build-push-action@v6
      with:
        push: true
        context: .
        platforms: linux/amd64,linux/arm64
        file: ${{ matrix.versions.distribution }}/Dockerfile
        tags: poad/docker-zsh:${{ matrix.versions.tag }}
        build-args: |
          DISTRIBUTION=${{ matrix.versions.distribution }}
          DIST_VERSION_CODE=${{ matrix.versions.dist_version_code }}
          UBUNTU_VERSION_CODE=${{ matrix.versions.ubuntu_version_code }}
